.connection
    a(href="https://observer-app.pro/" class="connection__link link") About App
    .connection__form
        h2.connection__title Observer
        p.connection__text Mentor login
        input(type="text" class="connection__input" id="address" value= context.currentAddress placeholder="ws://server.com")
        .connection__check-section 
            a(href="/").connection__check-button Check
            .connection__status
                img(src= context.isOnline ? './public/img/status-online.svg' : './public/img/status-offline.svg')
                span.connection__text-status= context.isOnline ? 'Server is available' : 'Server is not available'
        button.button.create-room(@click="room_create" disabled=!context.isOnline) Create room